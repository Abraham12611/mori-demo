{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare yarn@4.9.4 --activate && yarn install --immutable",
  "buildCommand": "yarn build"
}
